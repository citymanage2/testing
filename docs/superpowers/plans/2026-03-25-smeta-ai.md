# Smeta AI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Построить полнофункциональное веб-приложение Smeta AI для автоматизации строительных смет с FastAPI-бэкендом, React-фронтендом и Claude API.

**Architecture:** FastAPI-бэкенд с async PostgreSQL (asyncpg + SQLAlchemy 2.0), фоновая обработка задач через asyncio.create_task(), React 18 + Vite SPA-фронтенд с Zustand-стейтом и Axios. Claude API используется для анализа документов и обогащения данных.

**Tech Stack:** Python 3.12, FastAPI 0.115, SQLAlchemy 2.0, asyncpg, Alembic, Anthropic SDK, openpyxl, fpdf2, React 18, TypeScript 5.6, Vite 5.4, Zustand 5.0, Axios 1.7.9, PostgreSQL 15.

---

## File Structure

```
smeta-ai/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── auth.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── project.py
│   │   ├── task.py
│   │   ├── task_input_file.py
│   │   ├── task_result.py
│   │   ├── task_version.py
│   │   ├── estimate_item.py
│   │   └── price.py
│   ├── schemas/
│   │   ├── auth.py
│   │   ├── task.py
│   │   ├── project.py
│   │   ├── estimate.py
│   │   └── admin.py
│   ├── routers/
│   │   ├── auth.py
│   │   ├── tasks.py
│   │   ├── projects.py
│   │   └── admin.py
│   └── services/
│       ├── claude_service.py
│       ├── task_processor.py
│       ├── excel_service.py
│       ├── pdf_service.py
│       ├── price_service.py
│       ├── snapshot_service.py
│       ├── optimization_service.py
│       └── analogue_service.py
├── alembic/
│   ├── env.py
│   └── versions/001_initial.py
├── frontend/
│   ├── src/
│   │   ├── main.tsx
│   │   ├── App.tsx
│   │   ├── api/client.ts
│   │   ├── store/auth.ts
│   │   ├── pages/
│   │   │   ├── Login.tsx
│   │   │   ├── TaskCreate.tsx
│   │   │   ├── TaskStatus.tsx
│   │   │   ├── EstimateView.tsx
│   │   │   └── Admin.tsx
│   │   └── components/
│   │       ├── Layout.tsx
│   │       ├── ProjectsSidebar.tsx
│   │       ├── TaskTypeSelector.tsx
│   │       ├── FileUpload.tsx
│   │       ├── StatusBadge.tsx
│   │       ├── VersionHistoryDrawer.tsx
│   │       ├── OptimizationChecklist.tsx
│   │       ├── AnaloguePanel.tsx
│   │       └── ProtectedRoute.tsx
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── requirements.txt
├── alembic.ini
├── .env.example
├── render.yaml
└── Dockerfile
```

---

## Task 1: Инфраструктура

**Files:**
- Create: `requirements.txt`
- Create: `.env.example`
- Create: `app/__init__.py`
- Create: `app/config.py`
- Create: `app/database.py`
- Create: `alembic.ini`
- Create: `alembic/env.py`

- [ ] **Step 1: Создать requirements.txt**

```
fastapi==0.115.5
uvicorn[standard]
sqlalchemy[asyncio]==2.0.*
asyncpg
alembic
anthropic
openpyxl==3.1.5
fpdf2>=2.8.0
python-jose[cryptography]
passlib[bcrypt]
python-multipart
slowapi
structlog
pydantic-settings
pypdf>=4.0
```

- [ ] **Step 2: Создать .env.example**

```env
DATABASE_URL=postgresql+asyncpg://user:pass@localhost/smeta_ai
ANTHROPIC_API_KEY=sk-ant-...
JWT_SECRET=your-256-bit-secret
JWT_EXPIRE_HOURS=24
USER_PASSWORD=...
ADMIN_PASSWORD=...
MAX_FILES_PER_REQUEST=5
MAX_FILE_SIZE_MB=50
SEARCH_CITY=Екатеринбург
VAT_RATE=20
```

- [ ] **Step 3: Создать app/config.py** (точно по ARCHITECTURE.md)

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    database_url: str
    anthropic_api_key: str
    jwt_secret: str
    jwt_expire_hours: int = 24
    user_password: str
    admin_password: str
    max_files_per_request: int = 5
    max_file_size_mb: int = 50
    search_city: str = "Екатеринбург"
    vat_rate: float = 20.0

    class Config:
        env_file = ".env"

settings = Settings()
```

- [ ] **Step 4: Создать app/database.py** (точно по ARCHITECTURE.md)

```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase
from app.config import settings

engine = create_async_engine(
    settings.database_url,
    pool_size=5, max_overflow=10, pool_recycle=300
)
SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

class Base(DeclarativeBase):
    pass

async def get_db():
    async with SessionLocal() as session:
        try:
            yield session
        finally:
            await session.close()
```

- [ ] **Step 5: Инициализировать Alembic**

```bash
alembic init alembic
```

Затем отредактировать `alembic/env.py` — добавить импорт моделей и async поддержку:

```python
from logging.config import fileConfig
from sqlalchemy.ext.asyncio import async_engine_from_config
from sqlalchemy import pool
from alembic import context
from app.config import settings
from app.database import Base
import app.models  # noqa: F401 — импортирует все модели

config = context.config
config.set_main_option("sqlalchemy.url", settings.database_url)

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata

def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
    with context.begin_transaction():
        context.run_migrations()

def do_run_migrations(connection):
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()

async def run_async_migrations() -> None:
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()

def run_migrations_online() -> None:
    import asyncio
    asyncio.run(run_async_migrations())

if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

- [ ] **Step 6: Commit**

```bash
git add requirements.txt .env.example app/config.py app/database.py app/__init__.py alembic.ini alembic/
git commit -m "feat: add infrastructure - config, database, alembic setup"
```

---

## Task 2: Модели данных

**Files:**
- Create: `app/models/__init__.py`
- Create: `app/models/user.py`
- Create: `app/models/project.py`
- Create: `app/models/task.py`
- Create: `app/models/task_input_file.py`
- Create: `app/models/task_result.py`
- Create: `app/models/task_version.py`
- Create: `app/models/estimate_item.py`
- Create: `app/models/price.py`
- Create: `alembic/versions/001_initial.py`

- [ ] **Step 1: Создать app/models/user.py** (точно по MODELS.md)

```python
import uuid
from datetime import datetime, timezone
from sqlalchemy import Integer, String, DateTime
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    role: Mapped[str] = mapped_column(String(10), nullable=False)
    password_hash: Mapped[str] = mapped_column(String(255), nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), nullable=False,
        default=lambda: datetime.now(timezone.utc)
    )

    tasks: Mapped[list["Task"]] = relationship(back_populates="user")
    projects: Mapped[list["Project"]] = relationship(back_populates="user")
```

- [ ] **Step 2: Создать app/models/project.py** (точно по MODELS.md)

```python
import uuid
from datetime import datetime, timezone
from sqlalchemy import String, Text, Integer, DateTime, ForeignKey
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

class Project(Base):
    __tablename__ = "projects"

    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name: Mapped[str] = mapped_column(String(255), nullable=False)
    description: Mapped[str | None] = mapped_column(Text, nullable=True)
    user_id: Mapped[int | None] = mapped_column(
        Integer, ForeignKey("users.id", ondelete="SET NULL"), nullable=True
    )
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), nullable=False,
        default=lambda: datetime.now(timezone.utc)
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), nullable=False,
        default=lambda: datetime.now(timezone.utc),
        onupdate=lambda: datetime.now(timezone.utc)
    )

    user: Mapped["User"] = relationship(back_populates="projects")
    tasks: Mapped[list["Task"]] = relationship(back_populates="project")
```

- [ ] **Step 3: Создать app/models/task.py** (точно по MODELS.md)

Включает TASK_TYPES и ALLOWED_MIME_TYPES как константы модуля.

- [ ] **Step 4: Создать app/models/task_input_file.py, task_result.py, task_version.py, estimate_item.py, price.py** — точно по MODELS.md.

- [ ] **Step 5: Создать app/models/__init__.py**

```python
from app.database import Base
from app.models.user import User
from app.models.project import Project
from app.models.task import Task
from app.models.task_input_file import TaskInputFile
from app.models.task_result import TaskResult
from app.models.task_version import TaskVersion
from app.models.estimate_item import EstimateItem
from app.models.price import PriceWork, PriceMaterial, PriceList

__all__ = [
    "Base", "User", "Project", "Task", "TaskInputFile",
    "TaskResult", "TaskVersion", "EstimateItem",
    "PriceWork", "PriceMaterial", "PriceList",
]
```

- [ ] **Step 6: Создать начальную миграцию**

```bash
alembic revision --autogenerate -m "initial"
```

Проверить сгенерированный файл в `alembic/versions/` — убедиться что все таблицы присутствуют.

- [ ] **Step 7: Commit**

```bash
git add app/models/ alembic/versions/
git commit -m "feat: add all data models and initial migration"
```

---

## Task 3: Аутентификация

**Files:**
- Create: `app/auth.py`
- Create: `app/schemas/auth.py`
- Create: `app/routers/auth.py`

- [ ] **Step 1: Создать app/auth.py** (точно по API.md)

```python
from datetime import datetime, timedelta, timezone
from jose import JWTError, jwt
from passlib.context import CryptContext
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from app.config import settings

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
bearer = HTTPBearer()

def create_access_token(user_id: int, role: str) -> str:
    expire = datetime.now(timezone.utc) + timedelta(hours=settings.jwt_expire_hours)
    return jwt.encode(
        {"sub": str(user_id), "role": role, "exp": expire},
        settings.jwt_secret, algorithm="HS256"
    )

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def get_password_hash(password: str) -> str:
    return pwd_context.hash(password)

class CurrentUser:
    def __init__(self, user_id: int, role: str):
        self.id = user_id
        self.role = role

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(bearer)
) -> CurrentUser:
    try:
        payload = jwt.decode(credentials.credentials, settings.jwt_secret, algorithms=["HS256"])
        return CurrentUser(user_id=int(payload["sub"]), role=payload["role"])
    except JWTError:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")

async def get_admin_user(current_user: CurrentUser = Depends(get_current_user)) -> CurrentUser:
    if current_user.role != "admin":
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Admin only")
    return current_user
```

- [ ] **Step 2: Создать app/schemas/auth.py**

```python
from pydantic import BaseModel

class LoginRequest(BaseModel):
    password: str

class LoginResponse(BaseModel):
    access_token: str
    role: str
    expires_in: int
```

- [ ] **Step 3: Создать app/routers/auth.py**

Логика: сравнить password с bcrypt-хэшами `settings.user_password` и `settings.admin_password`.
Вернуть JWT с payload `{sub: "1" | "2", role: "user" | "admin"}`.

```python
from fastapi import APIRouter, HTTPException, status
from app.auth import create_access_token, get_password_hash, verify_password
from app.config import settings
from app.schemas.auth import LoginRequest, LoginResponse

router = APIRouter()

# Генерировать хэши при первом запуске — или использовать lazy comparison
_user_hash = None
_admin_hash = None

def _get_user_hash() -> str:
    global _user_hash
    if _user_hash is None:
        _user_hash = get_password_hash(settings.user_password)
    return _user_hash

def _get_admin_hash() -> str:
    global _admin_hash
    if _admin_hash is None:
        _admin_hash = get_password_hash(settings.admin_password)
    return _admin_hash

@router.post("/login", response_model=LoginResponse)
async def login(body: LoginRequest):
    if verify_password(body.password, _get_admin_hash()):
        token = create_access_token(2, "admin")
        return LoginResponse(access_token=token, role="admin", expires_in=settings.jwt_expire_hours * 3600)
    if verify_password(body.password, _get_user_hash()):
        token = create_access_token(1, "user")
        return LoginResponse(access_token=token, role="user", expires_in=settings.jwt_expire_hours * 3600)
    raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid password")
```

- [ ] **Step 4: Commit**

```bash
git add app/auth.py app/schemas/ app/routers/auth.py
git commit -m "feat: add JWT authentication"
```

---

## Task 4: Роутеры (Tasks, Projects, Admin)

**Files:**
- Create: `app/schemas/task.py`
- Create: `app/schemas/project.py`
- Create: `app/schemas/estimate.py`
- Create: `app/schemas/admin.py`
- Create: `app/routers/tasks.py`
- Create: `app/routers/projects.py`
- Create: `app/routers/admin.py`

- [ ] **Step 1: Создать app/schemas/task.py**

```python
from datetime import datetime
from pydantic import BaseModel

class TaskStatusResponse(BaseModel):
    id: str
    task_type: str
    status: str
    progress_message: str | None
    error_message: str | None
    estimate_status: str | None
    created_at: datetime
    updated_at: datetime

class MessageRequest(BaseModel):
    content: str

class TaskResultFile(BaseModel):
    id: int
    file_name: str
    mime_type: str
    created_at: datetime
```

- [ ] **Step 2: Создать app/schemas/project.py, estimate.py, admin.py** по API.md.

- [ ] **Step 3: Создать app/routers/tasks.py** — реализовать все 6 эндпоинтов по API.md:
  - `POST /tasks` — с валидацией TASK_TYPES, лимитов файлов, MIME-типов
  - `GET /tasks/{task_id}/status`
  - `POST /tasks/{task_id}/cancel`
  - `POST /tasks/{task_id}/message`
  - `GET /tasks/{task_id}/results`
  - `GET /results/{file_id}/download`

- [ ] **Step 4: Создать app/routers/projects.py** — все эндпоинты по API.md включая estimate, versions, optimize, analogues.

- [ ] **Step 5: Создать app/routers/admin.py** — все admin эндпоинты по API.md.

- [ ] **Step 6: Commit**

```bash
git add app/schemas/ app/routers/
git commit -m "feat: add tasks, projects, admin routers"
```

---

## Task 5: Сервисы

**Files:**
- Create: `app/services/claude_service.py`
- Create: `app/services/price_service.py`
- Create: `app/services/task_processor.py`
- Create: `app/services/excel_service.py`
- Create: `app/services/pdf_service.py`
- Create: `app/services/snapshot_service.py`
- Create: `app/services/optimization_service.py`
- Create: `app/services/analogue_service.py`

- [ ] **Step 1: Создать app/services/claude_service.py** — точно по SERVICES.md (retry, exponential backoff, web_search поддержка).

- [ ] **Step 2: Создать app/services/price_service.py** — точно по SERVICES.md (in-memory cache, difflib fuzzy search, Claude fallback).

- [ ] **Step 3: Создать app/services/excel_service.py** — точно по SERVICES.md (generate_list + generate_smeta с форматированием).

- [ ] **Step 4: Создать app/services/pdf_service.py** — точно по SERVICES.md (fpdf2, generate_comparison_report).

- [ ] **Step 5: Создать app/services/snapshot_service.py** — точно по SERVICES.md (save_snapshot + restore_snapshot).

- [ ] **Step 6: Создать app/services/optimization_service.py** — точно по SERVICES.md (get_optimization_plan + execute_optimization с батчами).

- [ ] **Step 7: Создать app/services/analogue_service.py** — точно по SERVICES.md (find_analogues с кэшем + apply/revert).

- [ ] **Step 8: Создать app/services/task_processor.py** — оркестратор по SERVICES.md:

Шаги processor:
1. `task.status = 'processing'`
2. Загрузить task_input_files
3. Декодировать файлы (`_decode_file`)
4. Сформировать system prompt по TASK_TYPES
5. Вызвать `claude_service.complete()`
6. Распарсить JSON (`_parse_claude_response`)
7. Обогатить ценами (`_enrich_with_prices`)
8. Bulk insert в estimate_items
9. Сгенерировать Excel/PDF (excel_service или pdf_service)
10. Сохранить в task_results
11. `task.status = 'completed'`, `task.estimate_status = 'calculated'`

При любом исключении: `task.status = 'failed'`, `task.error_message = str(e)`.

- [ ] **Step 9: Commit**

```bash
git add app/services/
git commit -m "feat: add all services - claude, price, processor, excel, pdf, snapshot, optimization, analogue"
```

---

## Task 6: app/main.py + финальная сборка бэкенда

**Files:**
- Create: `app/main.py`

- [ ] **Step 1: Создать app/main.py** (точно по ARCHITECTURE.md)

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.database import engine
from app.models import Base
from app.services.price_service import price_service
from app.routers import auth, tasks, projects, admin

@asynccontextmanager
async def lifespan(app: FastAPI):
    await price_service.load_cache()
    yield
    await engine.dispose()

app = FastAPI(title="Smeta AI", lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=False,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(auth.router, prefix="/auth", tags=["auth"])
app.include_router(tasks.router, prefix="/tasks", tags=["tasks"])
app.include_router(projects.router, prefix="/projects", tags=["projects"])
app.include_router(admin.router, prefix="/admin", tags=["admin"])

@app.get("/health")
async def health():
    return {"status": "ok"}
```

- [ ] **Step 2: Проверить запуск**

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
# Ожидание: сервер запустился, GET /health → {"status": "ok"}
```

- [ ] **Step 3: Commit**

```bash
git add app/main.py
git commit -m "feat: add main.py, wire all routers"
```

---

## Task 7: Frontend — инфраструктура

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/tsconfig.json`
- Create: `frontend/vite.config.ts`
- Create: `frontend/src/main.tsx`
- Create: `frontend/src/api/client.ts`
- Create: `frontend/src/store/auth.ts`
- Create: `frontend/src/App.tsx`
- Create: `frontend/src/components/ProtectedRoute.tsx`

- [ ] **Step 1: Создать frontend/package.json** (точно по FRONTEND.md)

- [ ] **Step 2: Создать frontend/tsconfig.json** с `strict: true` (точно по FRONTEND.md)

- [ ] **Step 3: Создать frontend/vite.config.ts** (точно по FRONTEND.md)

- [ ] **Step 4: Создать frontend/src/api/client.ts** — Axios instance + JWT interceptor + 401 logout (точно по FRONTEND.md)

- [ ] **Step 5: Создать frontend/src/store/auth.ts** — Zustand с persist (точно по FRONTEND.md)

- [ ] **Step 6: Создать frontend/src/components/ProtectedRoute.tsx** (точно по FRONTEND.md)

- [ ] **Step 7: Создать frontend/src/App.tsx** — маршрутизация (точно по FRONTEND.md)

- [ ] **Step 8: Установить зависимости и проверить**

```bash
cd frontend && npm install && npm run build
# Ожидание: build успешен без TypeScript-ошибок
```

- [ ] **Step 9: Commit**

```bash
git add frontend/
git commit -m "feat: add frontend infrastructure - vite, axios, zustand, router"
```

---

## Task 8: Frontend — страницы

**Files:**
- Create: `frontend/src/pages/Login.tsx`
- Create: `frontend/src/pages/TaskCreate.tsx`
- Create: `frontend/src/pages/TaskStatus.tsx`
- Create: `frontend/src/pages/EstimateView.tsx`
- Create: `frontend/src/pages/Admin.tsx`
- Create: `frontend/src/components/Layout.tsx`
- Create: `frontend/src/components/ProjectsSidebar.tsx`
- Create: `frontend/src/components/TaskTypeSelector.tsx`
- Create: `frontend/src/components/FileUpload.tsx`
- Create: `frontend/src/components/StatusBadge.tsx`
- Create: `frontend/src/components/VersionHistoryDrawer.tsx`
- Create: `frontend/src/components/OptimizationChecklist.tsx`
- Create: `frontend/src/components/AnaloguePanel.tsx`

- [ ] **Step 1: Login.tsx** — точно по FRONTEND.md (форма пароля, POST /auth/login, редирект по роли)

- [ ] **Step 2: Layout.tsx** — хедер + ProjectsSidebar + Outlet

- [ ] **Step 3: StatusBadge.tsx** — точно по FRONTEND.md (STATUS_CONFIG с цветами)

- [ ] **Step 4: FileUpload.tsx** — drag-and-drop, проверка count ≤ 5, size ≤ 50MB, список файлов с удалением

- [ ] **Step 5: TaskTypeSelector.tsx** — select с TASK_TYPES (12 типов)

- [ ] **Step 6: TaskCreate.tsx** — точно по FRONTEND.md (FormData, POST /tasks, navigate к status)

- [ ] **Step 7: TaskStatus.tsx** — polling (2s при processing, 5s иначе), чат-интерфейс, кнопки скачать/открыть смету

- [ ] **Step 8: EstimateView.tsx** — таблица позиций, итоги с НДС, кнопки История/Оптимизировать/Аналоги

- [ ] **Step 9: ProjectsSidebar.tsx** — список проектов, создание, drag задач в проект

- [ ] **Step 10: VersionHistoryDrawer.tsx** — список версий + restore с confirm

- [ ] **Step 11: OptimizationChecklist.tsx** — получить план → чеклист → execute

- [ ] **Step 12: AnaloguePanel.tsx** — find-analogues → карточки → apply/revert

- [ ] **Step 13: Admin.tsx** — вкладки: задачи (фильтры + таблица), прайс-листы (upload)

- [ ] **Step 14: Build check**

```bash
cd frontend && npm run build
# Ожидание: 0 TypeScript-ошибок, build успешен
```

- [ ] **Step 15: Commit**

```bash
git add frontend/src/
git commit -m "feat: add all frontend pages and components"
```

---

## Task 9: Конфигурация деплоя

**Files:**
- Create: `render.yaml`
- Create: `Dockerfile`

- [ ] **Step 1: Создать render.yaml** (точно по ARCHITECTURE.md)

- [ ] **Step 2: Создать Dockerfile** (точно по ARCHITECTURE.md)

- [ ] **Step 3: Commit**

```bash
git add render.yaml Dockerfile
git commit -m "feat: add render.yaml and Dockerfile for deployment"
```

---

## Финальная проверка

- [ ] Бэкенд: `uvicorn app.main:app` стартует без ошибок
- [ ] `GET /health` → `{"status": "ok"}`
- [ ] Миграция: `alembic upgrade head` создаёт все таблицы
- [ ] Frontend: `npm run build` без TypeScript-ошибок
- [ ] `POST /auth/login` с верным паролем → JWT токен
- [ ] `POST /auth/login` с неверным паролем → 401
